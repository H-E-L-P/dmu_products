; this script is a very basic wrapper to allow the IDL code to be run from the command line
.r RadialDependence

; this collects all compiled IDL subroutines and chucks them in a sub-directory both for portabaility and for transparency of version 

archive_software,'code'


exit

